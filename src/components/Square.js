import React from 'react'

const Square = ({value}) => {
    
    return (
            <button type="button">{value}</button>  //what if instead of 1 f we have dynamic value//
    )
};

export default Square
